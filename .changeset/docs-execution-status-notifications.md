---
---

docs: document the real-time execution-status feature in `docs.allma.dev` — the step `checkpoint`
field and progress model (flow definition reference), the `GET /flow-executions/{id}/progress`
endpoint with single/tree modes (execution-monitoring API), the per-trigger `notificationConfig` and
root-execution tracking on `START_FLOW_EXECUTION`, the sub-flow execution-tree roll-up on
`START_SUB_FLOW`, and the execution-tree + Admin-progress cross-references on the Execution Status
Notifications page.
